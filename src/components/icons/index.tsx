import AnnotationIconPath from './Annotation';
import LocationIconPath from './Location';
import PhoneIconPath from './Phone';
import ProcessIconPath from './Process';
import RefreshIconPath from './Refresh';
import SearchIconPath from './Search';
import StarIconPath from './Star';
import CheckIconPath from './Check';
import CheckCircleIconPath from './CheckCircle';
import CheckBadgeIconPath from './CheckBadge';
import CheckClipboardIconPath from './CheckClipboard';
import XIconPath from './X';
import XCircleIconPath from './XCircle';
import ThumbIconPath from './Thumb';
import UserIconPath from './User';
import ClipboardIconPath from './Clipboard';
import AtIconPath from './At';

export const iconIndex = {
  Annotation: AnnotationIconPath,
  Location: LocationIconPath,
  Phone: PhoneIconPath,
  Process: ProcessIconPath,
  Refresh: RefreshIconPath,
  Search: SearchIconPath,
  Star: StarIconPath,
  Check: CheckIconPath,
  CheckCircle: CheckCircleIconPath,
  CheckBadge: CheckBadgeIconPath,
  CheckClipboard: CheckClipboardIconPath,
  X: XIconPath,
  XCircle: XCircleIconPath,
  Thumb: ThumbIconPath,
  User: UserIconPath,
  Clipboard: ClipboardIconPath,
  At: AtIconPath,
};
